# Optional per-client settings.
# The client id used in hostname prefixes is the filename (without .env).
#
# CASINO_ID is reserved for a later /api/game/url-extended multi-client stage.
# CASINO_ID="clientname42"
